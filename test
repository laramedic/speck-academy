<!DOCTYPE html>
<html lang="en">

  <head>
    <title>Speck Academy - zadatak</title>
    <meta charset="utf-8">
    <meta name="autor" content="Lara Medić">
    <meta name="datum" content="15.02.2021">

    <style>
      body {
        background-color: #dfe3ee;
        font-family: Arial, Helvetica;
        text-align: center;
        font-size: 20px;
      }

      h1 {
        background-color: #4d648d;
        color: white;
        padding: 10px 15px;
      }

      ul {
        display: center;
        flex-direction: row;
        padding-left: 0;
        cursor: pointer;
        margin-bottom: 0;

      }

      li {
        margin-bottom: 5px;
        padding: 10px 15px;
        background-color: #8b9dc3;
        color: white;
        border: 1px solid;
        box-shadow: -3px 6px 4px;
      }

      li:hover {
        background-color: white;
        color: #4d648d;

      }

      .button {
        background-color: #1e1f26;
        border: none;
        color: white;
        padding: 12px 52px;
        text-align: center;
        display: inline-block;
        font-size: 16px;
        margin: 2px 2px;
        cursor: pointer;
      }

      #unos {
        font-size: 16px;
        margin: 2px 2px;
        padding: 10px 52px;
      }

      #prodTitle {
        position: relative;
        left: -200px;
        font-size: 18px;
      }

      #numProd {
        position: relative;
        left: -75px;
        font-size: 18px;
      }

      #remove {
        position: relative;
        left: 73.5px;
        font-size: 18px;
      }

    </style>

    <script type='text/javascript'>
      function addProduct() {
        var ul = document.getElementById("lista");
        var unos = document.getElementById("unos");
        var li = document.createElement("li");
        li.setAttribute('id', unos.value);
        li.appendChild(document.createTextNode(unos.value));
        ul.appendChild(li);
      }

      function removeProducts() {
        var ul = document.getElementById("lista");
        var unos = document.getElementById("unos");
        var item = document.getElementById(unos.value);
        ul.removeChild(item);
      }

      function countItems() {
        var ul = document.getElementById("unos");
        var i = 0,
          itemCount = 0;
        while (ul.getElementsByTagName('li')[i++]) itemCount++;
        document.write(itemCount);
      }

    </script>
  </head>

  <body>

    <h1>Products App</h1>

    <div id="prodTitle">
      <p>Product title:</p>
    </div>

    <input type="text" id="unos" placeholder="Insert product title" onfocus="this.value=''" required>
    <button onclick="addProduct()" type="button" class="button">Add product</button>

    <br><br>

    <label for="numProd" id="numProd">Number of products: <script>
        countItems()

      </script> </label>

    <a href="#" id="remove" onclick="removeProducts()">Remove all products</a>

    <ul id="lista">
    </ul>

  </body>

</html>
